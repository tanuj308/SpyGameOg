[build]
  base = "frontend"
  command = "npm install && npm run build"
  publish = "dist"

# SPA redirect: send all routes to index.html for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
